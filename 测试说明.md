# 模块 C 测试说明

测试内容：模块 C 文件分片传输、CRC32 校验、断点续传、上传续传、进度输出、样例文件 MD5 对账，以及项目可编译/可运行性检查。

## 结论

模块 C 的核心功能需求已通过现有自动化测试验证，Android JVM 单测、Python 服务层单测、5MB/50MB/500MB 样例 MD5 对账和 Android debug 构建均已跑通，无测试断言失败和构建错误。

## 测试环境

- Python：Codex bundled Python
- Android：Gradle Wrapper `android\gradlew.bat`
- Android SDK

## 执行记录

| 序号 | 测试项 | 命令 | 结果 |
| --- | --- | --- | --- |
| 1 | Python 服务层全量单测 | `python -m unittest discover -s tests -p "test_*.py" -v` | 通过，17 tests，0 failures，0 errors |
| 2 | 模块 C 样例 MD5 对账 | `python test-assets\smoke\module_c_md5_check.py` | 通过，5MB/50MB/500MB 三档均 OK |
| 3 | Android JVM 单测 | `android\gradlew.bat testDebugUnitTest --rerun-tasks` | 通过，BUILD SUCCESSFUL，22 tasks executed |
| 4 | Android debug 构建 | `android\gradlew.bat assembleDebug` | 通过，生成 `app-debug.apk`，大小 16,923,185 bytes |

## Android 单测明细

| 测试类 | 用例数 | failures | errors | skipped |
| --- | ---: | ---: | ---: | ---: |
| `com.linkflow.app.file.ChunkVerifierTest` | 2 | 0 | 0 | 0 |
| `com.linkflow.app.file.FileTransferManagerTest` | 4 | 0 | 0 | 0 |
| `com.linkflow.app.nsd.NsdDiscoveryManagerTest` | 1 | 0 | 0 | 0 |
| `com.linkflow.app.rpc.RpcClientTest` | 2 | 0 | 0 | 0 |

模块 C 直接相关的 Android 文件传输用例覆盖：

- 256KB 固定分片读取。
- 下载写入 `.part` 并维护 `.offset`。
- 进度从 0 输出到 100。
- 下载断点续传，已确认分片不重复拉取。
- CRC32 校验失败后重试耗尽并清理 `.part/.offset`。
- 上传断点续传，已确认分片不重复上传。
- `ChunkVerifier` CRC32 计算与校验。

## Python 单测明细

Python 单测共执行 17 项，全部通过。与模块 C/文件传输相关的重点覆盖：

- `test_file_chunk_roundtrip`：服务端文件分片读取与 eof 标记。
- `test_get_file_chunk`：Web 消息入口获取文件分片并返回 base64 数据。
- `test_file_list_request_roundtrip`：文件列表请求链路可用。
- 其余用例覆盖加密帧、RPC 配对、心跳、剪贴板、allowlist、mDNS 等相邻链路，确认项目主干运行无 import/runtime 错误。

## 样例文件对账

| 样例 | 大小 | 分片数 | MD5 结果 |
| --- | ---: | ---: | --- |
| `module_c_5mb.bin` | 5,242,880 | 20 | OK |
| `module_c_50mb.bin` | 52,428,800 | 200 | OK |
| `module_c_500mb.bin` | 524,288,000 | 2000 | OK |

## 需求验收映射

| 模块 C 需求 | 验证方式 | 状态 |
| --- | --- | --- |
| 256KB 固定分片传输 | Android `FileTransferManagerTest` 校验每次 `file.read_chunk` size 为 `262144` | 通过 |
| CRC32 校验 | Android `ChunkVerifierTest` 与下载/上传分片校验 | 通过 |
| 下载断点续传 | Android `download_resumes_from_saved_offset_without_refetching_confirmed_chunks` | 通过 |
| 上传断点续传 | Android `upload_sends_verified_chunks_and_resumes_from_offset` | 通过 |
| CRC 失败最多重试并清理临时状态 | Android `download_deletes_part_and_offset_after_crc_retry_exhaustion` | 通过 |
| 进度输出 0 到 100 | Android 下载/上传用例断言 `progress.first()==0`、`progress.last()==100` | 通过 |
| 5MB/50MB/500MB 集成样例 | `module_c_md5_check.py` 对账三档样例 MD5 | 通过 |
| 代码可编译运行无报错 | `testDebugUnitTest --rerun-tasks` 与 `assembleDebug` | 通过 |
