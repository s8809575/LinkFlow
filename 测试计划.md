# LinkFlow 测试计划（单元 / 集成 / 端到端）

目标：对当前代码库已实现的功能进行可重复验证，并将覆盖率量化到可验收标准。

## 1. 测试范围

### 1.1 覆盖的系统能力

- PC（Python）：TCP JSON-RPC（8089）+ AES-128-GCM + mDNS + 文件分片 + 系统状态 + 应急控制 + 文本剪贴板 + WebSocket 网桥（8766）
- Android：Nsd 发现 + 扫码/手输配对 + EncryptedSharedPreferences + TCP 长连接（心跳/重连）+ RPC 超时重试 + 文件断点续传（下载）
- PC Web：WebSocket 拉目录/拉分片下载

### 1.2 不在本轮测试范围

- 屏幕快照（`get_screen_snapshot` 仍为空实现）
- Windows 通知真实弹窗（`notify.push` 目前为回显）
- 手机端完整 UI 原型一致性（当前 Android 仅有配对页，不含文件管理/控制台界面）

## 2. 质量门槛（验收标准）

- Python：行覆盖率 `>=80%`，分支覆盖率 `>=75%`
- Android（Unit）：行覆盖率 `>=80%`，分支覆盖率 `>=75%`
- Android（Instrumentation / E2E）：关键路径用例全部通过（至少 10 条）
- 关键性能基线（本地热点同网段）：
  - RPC 调用 P95 <= 500ms（不含文件传输）
  - 文件分片下载稳定吞吐 >= 8MB/s（以 256KB 分片连续拉取测得）

## 3. 测试环境配置

### 3.1 PC 端环境

- OS：Windows 10/11
- Python：3.13.x（项目当前使用）
- 依赖：`pip install -r requirements.txt`
- 可选：安装 `coverage`（用于覆盖率统计）

### 3.2 Android 环境

- Android Studio（任意稳定版）
- Gradle 工程：`android/`
- 设备：Android 10+ 真机（建议至少 10/12/13 各 1 台）
- 权限：相机（扫码）、Wi-Fi/网络（Nsd/Socket）

### 3.3 网络拓扑

- 电脑开热点 → 手机连接热点 → 同网段直连
- mDNS：允许 multicast（部分系统策略会阻断，需要在排障清单中注明）

## 4. 测试数据准备（fixtures）

### 4.1 文件传输样本

- 生成文件：
  - 5MB：`demo_5mb.bin`
  - 50MB：`demo_50mb.bin`
  - 500MB：`demo_500mb.bin`
- 验证字段：
  - 下载完成后 MD5（Android 侧）与源文件一致
  - `.offset` 文件存在且随进度增长

### 4.2 RPC 固定响应样例

- `test-assets/fixtures/*.json` 作为模块 mock 与冒烟联通的最小样例

## 5. 单元测试（Unit Tests）

### 5.1 Python 单元测试

- 入口：`python -m unittest discover -s tests -v`
- 已覆盖的关键用例（示例映射）：
  - AES-GCM roundtrip：`tests/test_rpc_crypto.py`
  - 帧封包/解包：`tests/test_rpc_framer.py`
  - 配对后加密请求：`tests/test_rpc_server_pairing.py`
  - 文件分片 roundtrip：`tests/test_file_chunk.py`
  - mDNS name 生成：`tests/test_mdns_service.py`

#### 预期结果验证标准

- 所有用例返回 `OK`
- 对错误分支：必须断言 `error.code` / `error.message`（避免仅检查“抛异常”）

#### 覆盖率统计（建议）

```bash
python -m pip install coverage
coverage run -m unittest discover -s tests
coverage report -m
coverage html
```

### 5.2 Android 单元测试

- 入口（示例）：
  - Windows PowerShell：`cd android; .\gradlew testDebugUnitTest`
- 当前建议覆盖点：
  - `RpcClient`：超时/异常→3次调用次数、toast 触发、rollback hook 调用
  - `ChunkVerifier`：CRC32 已知值校验
  - `NsdDiscoveryManager`：Discoverer 注入后重试次数与失败码

#### 覆盖率统计（建议）

- 建议接入 Jacoco（配置到 `android/app/build.gradle`），产出：
  - `jacocoTestReport`（HTML/XML）

## 6. 集成测试（Integration Tests）

### 6.1 Python 集成（不依赖 Android）

目标：验证 PC 端 RPC Server 在真实 socket 下的行为与状态转换。

#### 用例 I-1：配对→加密→system.stats

- Given PC 启动 `python main.py`
- When 客户端建立 TCP 到 `8089`，调用 `pair.bind`，再调用 `system.stats`
- Then `system.stats` 返回 `result.cpu_percent/memory_percent/os_info` 且无 `error`

#### 用例 I-2：file.stat + file.read\_chunk 连续拉取

- Given PC 存在测试文件 `demo_50mb.bin`
- When 调 `file.stat` 获取 size，循环 `file.read_chunk(offset,size=262144)` 直到 eof
- Then 每片 `crc32` 与客户端计算一致，最终拼接文件 MD5 一致

### 6.2 Android 集成（Instrumentation）

目标：验证 Android 真实使用 NsdManager/Socket/加密通道。

#### 用例 AI-1：Nsd 发现成功路径

- Given 手机已连接 PC 热点
- When 启动 App 点击“发现电脑”
- Then 5 秒内显示 IPv4；pairing\_id 自动写入输入框

#### 用例 AI-2：配对成功路径（扫码/手输任选其一）

- Given pairing\_id/key\_b64 正确
- When 点击“一键配对”
- Then toast 显示“配对成功”，并能立即调用一次 `system.stats` 返回 result

#### 用例 AI-3：断线→重连最多 5 次

- Given 已配对且心跳进行中
- When 关闭热点 2 分钟再打开
- Then App 在 5 次重连内恢复；若恢复失败应出现 toast “无法恢复连接”

## 7. 端到端测试（E2E）

### 7.1 E2E-1：手机发现→配对→下载 5MB

- Given 热点/同网段可用，PC 启动 `main.py`
- When Android 发现电脑并配对，触发下载（使用 `FileTransferManager.download`）
- Then 下载完成文件 MD5 与源文件一致，`.offset` 被清理且最终文件存在

### 7.2 E2E-2：断点续传（杀进程）

- Given 开始下载 50MB
- When 下载到 30% 强制杀掉 App，重启后继续
- Then 从 `.offset` 继续增长，最终 MD5 一致

### 7.3 E2E-3：PC Web 控制台拉目录/下载

- Given PC 启动 `main.py`
- When 打开 `web_pc/index.html`，执行目录浏览与下载
- Then 页面显示 `sys_stats` 持续更新；下载文件内容正确

## 8. 一键冒烟（合并前检测意外耦合）

- 脚本：`python test-assets/smoke/run_smoke.py --fixtures test-assets/fixtures --src-root .`
- 预期：输出 `SMOKE PASSED`

## 9. 输出物（测试交付）

- 覆盖率报告：
  - Python：`coverage html` 生成的 `htmlcov/`
  - Android：Jacoco HTML/XML
- 集成/E2E 报告：
  - Android instrumentation JUnit report + logcat 关键日志
  - 文件传输吞吐/延迟统计（csv 或文本均可）

