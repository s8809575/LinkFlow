# 模块C文件传输设计说明

## 1. 设计目标

模块C负责 LinkFlow 的文件分片传输能力，边界限定为“已建立 RPC 会话后的文件上传、下载、完整性校验、断点续传和进度输出”。模块C不处理服务发现、安全配对、设备连接管理、系统控制命令、UI 或云端对象存储适配。

核心交付类：

- `FileTransferManager.kt`
- `ChunkVerifier.kt`

核心冻结接口：

```kotlin
fun download(req: FileTransferRequestDTO, target: File): Flow<Int>
fun upload(req: FileTransferRequestDTO, source: File): Flow<Int>
fun verifyChunk(bytes: ByteArray, expectedCrc32: Long): Boolean
fun loadOffset(path: String): Long
fun saveOffset(path: String, offset: Long): Unit
```

## 2. 模块边界

模块C只依赖模块B提供的 `RpcClient` 能力发送 JSON-RPC 消息。输入为 `RpcSessionDTO` 和业务触发的 `FileTransferRequestDTO`，输出为进度 `Flow<Int>` 以及按 IDL-v1.0.0 约定表达的传输结果语义。

跨模块约束：

- 仅通过冻结 DTO/IDL 和 JSON-RPC 消息交互。
- 不新增隐式字段。
- 不引入全局可变状态、单例共享缓存或隐式事件总线。
- 不 import 模块A、模块B、模块D的内部实现细节；模块C只使用 RPC 抽象能力。

## 3. 数据结构

IDL-v1.0.0 相关 DTO：

```json
{
  "FileTransferRequestDTO": {
    "schema_version": "1.0.0",
    "path": "string",
    "offset": 0,
    "size": 262144,
    "direction": "download|upload"
  },
  "FileTransferResultDTO": {
    "schema_version": "1.0.0",
    "path": "string",
    "offset": 0,
    "size": 0,
    "crc32": 0,
    "eof": false,
    "ok": true,
    "error_code": "string|null"
  }
}
```

模块C实现中固定分片大小为 `256 * 1024` 字节。即使外部请求传入更大的 `size`，实际 RPC 拉取或上传分片也会限制在 256KB 以内。

## 4. 下载流程

下载由 `FileTransferManager.download(req, target)` 执行，输出 `0..100` 的 `Flow<Int>`。

流程：

1. 校验 `schemaVersion == "1.0.0"`，校验 `direction == "download"`。
2. 调用 `file.stat` 获取远端文件总大小。
3. 读取本地 `target.absolutePath + ".offset"`。
4. 若存在有效 offset，则从该确认点恢复；否则使用请求中的 `req.offset`。
5. 使用 `target.absolutePath + ".part"` 作为临时下载文件。
6. 按 256KB 调用 `file.read_chunk`。
7. 对返回的 `data_b64` 解码，并用返回的 `crc32` 校验。
8. 校验通过后写入 `.part`，刷新并保存新的 `.offset`。
9. 根据已确认 offset 计算并发送进度。
10. 完成后将 `.part` 原子化替换为目标文件，删除 `.offset`。

下载 RPC 契约：

```json
{
  "jsonrpc": "2.0",
  "id": 9,
  "method": "file.read_chunk",
  "params": {
    "path": "C:/a.bin",
    "offset": 0,
    "size": 262144
  }
}
```

响应只读取 IDL 中冻结字段：

```json
{
  "path": "C:/a.bin",
  "offset": 0,
  "size": 262144,
  "crc32": 123456789,
  "eof": false,
  "data_b64": "..."
}
```

## 5. 上传流程

上传由 `FileTransferManager.upload(req, source)` 执行，输出 `0..100` 的 `Flow<Int>`。

流程：

1. 校验 `schemaVersion == "1.0.0"`，校验 `direction == "upload"`。
2. 校验本地 `source` 为文件。
3. 读取 `source.absolutePath + ".offset"` 作为已确认上传点。
4. 使用 `RandomAccessFile` 跳转到 offset。
5. 每次读取不超过 256KB。
6. 计算分片 CRC32。
7. 通过 JSON-RPC 发送分片写入请求。
8. RPC 返回成功后才保存新的 `.offset`。
9. 全部上传完成后删除 `.offset` 并发送 100。

当前上传侧使用 `file.write_chunk` 作为分片写入 RPC 方法名，参数保持分片传输必要字段：

```json
{
  "path": "C:/a.bin",
  "offset": 0,
  "size": 262144,
  "crc32": 123456789,
  "eof": false,
  "data_b64": "..."
}
```

## 6. CRC32 校验

`ChunkVerifier` 使用 JDK 标准库 `java.util.zip.CRC32`。

校验规则：

- 发送端对原始分片 bytes 计算 CRC32。
- 接收端对解码后的 bytes 重新计算 CRC32。
- `verifyChunk(bytes, expectedCrc32)` 将 `expectedCrc32` 视为 unsigned 32-bit 范围比较。
- 仅校验通过的分片会被持久化为已确认进度。

## 7. 断点续传

模块C使用两个本地文件实现恢复：

- `.part`：尚未完成的下载临时文件。
- `.offset`：已被确认的字节偏移。

恢复策略：

- 进程被杀、断网或 RPC 中断后，重新调用同一传输方法会读取 `.offset`。
- 若 `.offset > 0`，优先信任本地确认点，而不是业务请求中的 offset。
- 下载时若 `.part` 长度小于 `.offset`，会回退到 `.part.length()`，避免跳过缺失数据。
- 若 `.offset` 大于远端或本地文件总大小，认为记录异常并清理 offset 后从 0 开始。

该策略保证不会重复上传或下载已确认分片，也不会把未写入成功的分片误记为完成。

## 8. 重试与失败处理

下载分片 CRC32 校验失败时，模块C会对同一 offset 最多额外重试 3 次。若仍失败：

- 删除 `.part`。
- 删除 `.offset`。
- 抛出失败异常。

只有 CRC 通过且分片写入成功后，才更新 `.offset`。这保证 offset 记录代表“已确认完成”的边界。

## 9. 进度流

`download` 和 `upload` 都返回 `Flow<Int>`：

- 初始发送 `0`。
- 每确认一个分片后发送当前百分比。
- 结束发送 `100`。
- 相同进度值会去重，避免重复噪声。

进度计算：

```text
progress = (confirmedOffset / totalSize * 100).toInt().coerceIn(0, 100)
```

空文件或 `totalSize <= 0` 时直接视为 100。

## 10. 安全与隔离

模块C不负责鉴权、配对或连接生命周期，这些能力属于模块A/B。模块C只在已经具备 RPC 能力的前提下工作。

实现中没有全局可变状态，没有静态缓存，没有事件总线。每个 `FileTransferManager` 实例只持有构造时注入的 `RpcClient`，传输状态由本地 `.part/.offset` 文件显式表达。

## 11. 相关文件

- `android/app/src/main/java/com/linkflow/app/file/FileTransferManager.kt`
- `android/app/src/main/java/com/linkflow/app/file/ChunkVerifier.kt`
- `android/app/src/test/java/com/linkflow/app/file/FileTransferManagerTest.kt`
- `android/app/src/test/java/com/linkflow/app/file/ChunkVerifierTest.kt`
- `test-assets/fixtures/module_c_transfer_samples.json`
- `test-assets/smoke/module_c_md5_check.py`
