# LinkFlow 功能清单（基于现有代码库）

本文档仅覆盖**当前代码库已实现**的核心功能模块、业务逻辑与系统特性（含 Python PC 端、Android 端、Web 控制台）。未实现/部分实现能力不在此文档展开。

## 1. 总体形态与端口

| 子系统 | 入口 | 端口 | 协议 | 说明 |
|---|---:|---:|---|---|
| PC 核心（Legacy TCP） | `core/server.py` | 5000 | 自定义二进制 Type+Length+Payload | 主要用于早期“手机端”接入与状态推送 |
| PC Web 网桥 | `core/utils.py` | 8766 | WebSocket + JSON | 供 `web_pc/index.html` 使用 |
| PC RPC 服务 | `core/rpc_server.py` | 8089 | TCP 长连接 + Length-Prefixed + JSON-RPC + AES-128-GCM | 当前“改验收”的主协议通道 |
| 服务发现 | `core/mdns_service.py` | 8089 | mDNS `_companion._tcp.local.` | TXT 记录包含 `pairing_id` |

## 2. PC 端（Python）核心模块

### 2.1 启动编排
- 入口：[main.py](file:///e:/SCL_love/LinkFlow/main.py)
- 核心行为：
  - 启动 Legacy TCP：`LinkFlowServer(host=0.0.0.0, port=5000)`
  - 启动 WebSocket：`WebBridge(port=8766)`
  - 启动 RPC：`LinkFlowRpcServer(host=0.0.0.0, port=8089, pairing_id, pairing_key)`
  - 广播 mDNS：`MdnsService(service_type=_companion._tcp.local., properties={"pairing_id": ...})`
  - 后台线程每 2 秒推送一次系统状态（TCP + WebSocket）

### 2.2 JSON-RPC（TCP 8089）方法面
- 实现文件：[core/rpc_server.py](file:///e:/SCL_love/LinkFlow/core/rpc_server.py)
- 帧格式：4 字节大端长度 + payload；配对成功后 payload 变为 AES-128-GCM 加密包（nonce12B+密文+tag）
- 方法列表（已实现）：
  - `pair.bind`：建立安全通道（配对成功后切换加密）
  - `system.stats`：返回系统状态（CPU/内存/电池/OS）
  - `system.control`：执行 `mute/sleep/screen_off/reboot`
  - `file.list`：目录列表（文件/目录/大小/mtime）
  - `file.stat`：返回文件大小
  - `file.read_chunk`：按 `offset/size` 读取切片（默认 256KB），返回 `crc32` 与 `data_b64`
  - `notify.push`：当前为回显（不触发系统通知）
  - `sys.heartbeat`：返回服务端时间戳

### 2.3 文件能力
- 实现文件：[core/file_service.py](file:///e:/SCL_love/LinkFlow/core/file_service.py)
- 已实现点：
  - `get_directory_info(path)`：扫描并按“目录优先、名称排序”
  - `read_file_chunk(path, offset, chunk_size)`：分片读取
  - `is_path_allowed(path, allowed_roots)`：白名单判定（当前主要在 WebSocket 侧接入）

### 2.4 系统状态与应急控制
- 实现文件：[core/monitor_service.py](file:///e:/SCL_love/LinkFlow/core/monitor_service.py)
- 已实现点：
  - `get_system_stats()`：读取 CPU/内存/电池/OS（psutil 缺失时返回 0）
  - `execute_control_command(cmd)`：Windows 指令执行
- 说明：`get_screen_snapshot()` 当前为空实现

### 2.5 剪贴板（Windows 文本）
- 实现文件：[core/clipboard_service.py](file:///e:/SCL_love/LinkFlow/core/clipboard_service.py)
- 已实现点：
  - `start_watching()`：每秒轮询，发现变化回调推送
  - `set_clipboard_text(text)`：写入 Windows 文本剪贴板（带同内容跳过）

### 2.6 mDNS 广播
- 实现文件：[core/mdns_service.py](file:///e:/SCL_love/LinkFlow/core/mdns_service.py)
- 已实现点：广播 `_companion._tcp.local.`，TXT 记录包含 `pairing_id`

### 2.7 热点控制（Windows best-effort）
- 实现文件：[core/hotspot_service.py](file:///e:/SCL_love/LinkFlow/core/hotspot_service.py)
- 已实现点：
  - `start()` / `stop()`：调用 `netsh wlan hostednetwork`（部分 Win10/11 可能不支持）

### 2.8 WebSocket 网桥（供 PC 网页端）
- 实现文件：[core/utils.py](file:///e:/SCL_love/LinkFlow/core/utils.py)
- 已实现点：
  - 多连接注册与广播
  - `on_message_hook` 回调（由 `main.py` 接入）
- Web 消息处理（已实现）：
  - `get_files`：目录列表
  - `control`：应急指令
  - `get_file_chunk`：分块读取并 base64 回传

## 3. PC 网页端（web_pc）
- 入口：[web_pc/index.html](file:///e:/SCL_love/LinkFlow/web_pc/index.html)
- 依赖：WebSocket `ws://localhost:8766`
- 已实现点：
  - 展示 `sys_stats`
  - 请求 `get_files` 并渲染列表
  - 请求 `get_file_chunk` 并进行浏览器端下载（基于 base64 分块）

## 4. Android 端（Android 10+）
代码根：`android/app/src/main/java/com/linkflow/app/`

### 4.1 服务发现（Nsd）
- [NsdDiscoveryManager.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/nsd/NsdDiscoveryManager.kt)
- 已实现点：
  - NsdManager discover `_companion._tcp.` 并 resolve
  - 解析 TXT `pairing_id` 与 IPv4
  - 失败重试（逻辑层可注入 Discoverer 做单测）

### 4.2 配对与密钥安全存储
- UI：[PairingActivity.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/pairing/PairingActivity.kt)
- 扫码：[QRScanFragment.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/pairing/QRScanFragment.kt)
- 存储：[SecurePrefs.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/prefs/SecurePrefs.kt)（EncryptedSharedPreferences）

### 4.3 TCP 长连接 + 加密通道
- [TcpConnectionManager.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/tcp/TcpConnectionManager.kt)
- 已实现点：
  - 明文 `pair.bind` 完成后切换 AES-GCM framer
  - 心跳 `sys.heartbeat`（30s）与断线重连（指数退避，最多 5 次）

### 4.4 RPC 统一入口
- [RpcClient.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/rpc/RpcClient.kt)
- 已实现点：
  - 2s 超时、3 次重试
  - 失败时执行 rollback hook 并 toast

### 4.5 文件下载（分片/CRC32/断点续传）
- [FileTransferManager.kt](file:///e:/SCL_love/LinkFlow/android/app/src/main/java/com/linkflow/app/file/FileTransferManager.kt)
- 已实现点：
  - 固定 256KB 请求（`file.read_chunk`）
  - CRC32 校验（不匹配会重拉当前分片，最多 3 次）
  - `.offset` 文件记录 offset，实现重启续传
  - `Flow<Int>` 输出进度

## 5. 已具备的自动化测试资产（现有）
### 5.1 Python（unittest）
- 路径：`tests/`
- 覆盖面：协议/加密/帧、RPC 配对、文件分片、Web 消息处理、mDNS、心跳、allowlist 等

### 5.2 Android（JUnit 单元）
- 路径：`android/app/src/test/`
- 已有：CRC32 校验、RPC 重试 toast、Nsd 重试逻辑
